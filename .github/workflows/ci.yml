name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev,mcp]"
      - name: Ruff
        run: ruff check neoxlink_sdk/catalog.py neoxlink_sdk/unspsc.py neoxlink_sdk/mcp_server.py neoxlink_sdk/open_source/taxonomy_mapping.py neoxlink_sdk/open_source/__init__.py tests/
      - name: Pytest
        run: python -m pytest
