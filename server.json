{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.neowalter/neoxlink",
  "title": "NEOXLINK MCP",
  "description": "MCP server: natural language to structured intent (UNSPSC where applicable), confirm, resolve; optional local match engine.",
  "version": "0.6.4",
  "websiteUrl": "https://github.com/neowalter/NEOXLINK-SDK",
  "repository": {
    "url": "https://github.com/neowalter/NEOXLINK-SDK",
    "source": "github"
  },
  "packages": [
    {
      "registryType": "pypi",
      "identifier": "neoxlink",
      "version": "0.6.4",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "NEOXLINK_API_KEY",
          "description": "Bearer token for the NeoXlink API (required for remote parse/confirm).",
          "format": "string",
          "isRequired": true,
          "isSecret": true
        },
        {
          "name": "NEOXLINK_BASE_URL",
          "description": "API base URL; default is production if unset.",
          "format": "string",
          "isRequired": false,
          "isSecret": false
        },
        {
          "name": "NEOXLINK_ENABLE_MATCH",
          "description": "Set to 1/true to register local neoxlink.match_intent.",
          "format": "string",
          "isRequired": false,
          "isSecret": false
        }
      ]
    }
  ]
}
